# resilient-state-sync-udp
Aether Sync is a resilient multi-master state synchronization system designed for unreliable mesh networks. It achieves eventual consistency over UDP using gossip protocols, vector clocks, Merkle trees, and CRDT-inspired conflict resolution.
